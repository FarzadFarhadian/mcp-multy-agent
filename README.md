# mcp-multy-agent
 It covers prerequisites, repository layout, and environment configuration (.env.sample) to keep secrets secure.  It guides installation, setup, and run steps for starting the MCP server and the frontend UI.  It explains usage patterns, common issues (like EOF/invalid JSON), and debugging with mcp_debug.log and MCP_LOG_LEVEL.
